# Личный кабинет клиента по экологии (EcologyLK)

## ⚠️ Контекст (Тестовое задание)

Этот проект является выполнением тестового задания, основной целью которого было создание полнофункционального MVP-приложения с помощью нейросети (ИИ).

Весь процесс разработки, от создания структуры проекта до реализации "движка правил" и финального рефакторинга, задокументирован в файле **`Project_AI_Log.md`**. Этот лог демонстрирует пошаговый процесс принятия решений ИИ.

**EcologyLK** — это полнофункциональное MVP веб-приложение (SPA), разработанное для ООО «ГлавПриродБюро». Оно предоставляет "Личный кабинет" для клиентов компании, автоматизируя формирование экологических требований и управляя связанными документами.

Проект состоит из двух частей:

- **EcologyLK.Api**: Backend-сервер на **.NET 9 (C#)**, предоставляющий REST API.
- **EcologyLK.Client**: Frontend-приложение на **Angular 20 (TypeScript)**.

## 🚀 Стек технологий

- **Backend**: .NET 9 Web API, ASP.NET Identity (для JWT), Entity Framework Core (с `InMemoryDatabase` для MVP).
- **Frontend**: Angular 20, TypeScript, Angular Signals, Reactive Forms, SCSS.
- **UI/UX**: FullCalendar (для Календаря событий).
- **Архитектура**: REST API, JWT-аутентификация, Row-Level Security (RLS).

## ✨ Ключевые функции (MVP)

Проект полностью реализует все 100% требований из `ТЗ_Личный_кабинет_клиента_по_экологии_MVP.docx.pdf`.

### 1. Основной функционал (для Клиента)

- **Анкета клиента**: Клиент (или Админ) заполняет "Анкету" (`/new-site`), указывая параметры площадки (Категория НВОС, Водопользование, Побочные продукты).
- **Динамическая "Карта требований"**: На основе "Анкеты", система автоматически генерирует "Карту требований" (согласно `Критерии_для_формирования_требований_.docx.pdf`).
- **Хранилище артефактов**: На странице площадки (`/site/:id`) реализована загрузка, скачивание и удаление файлов (артефактов), привязанных к площадке.
- **Интерактивный "Календарь событий"**: На странице (`/calendar`) с помощью **FullCalendar** отображаются все дедлайны из "Карты требований", окрашенные в зависимости от статуса (выполнено, в работе, просрочено).
- **Блок "Риски"**: В "Карту требований" автоматически добавляется информация о штрафах (КоАП).
- **Блок "Договора/счета/акты"**: На странице площадки отображается список связанных финансовых документов.

### 2. Система безопасности (ТЗ п.4, п.6)

- **Аутентификация (JWT)**: Реализована полная система входа (`/login`) с использованием JWT-токенов.
- **Авторизация по Ролям**: Четкое разделение на `Admin` (полный доступ) и `Client` (ограниченный доступ).
- **Row-Level Security (RLS)**: **Критически важный** функционал. Пользователь с ролью `Client` может видеть данные **только** тех юрлиц (`ClientId`), к которым он привязан. Админ видит все.

### 3. Админ-панель (ТЗ п.7)

Администраторам (`admin@ecology.lk`) доступны защищенные разделы для полного управления системой:

- **Управление ЮрЛицами**: (`/admin/clients`) Создание и просмотр компаний-клиентов.
- **Управление Пользователями**: (`/admin/users`) Создание и просмотр пользователей, их привязка к ЮрЛицам и назначение ролей.
- **Ручная правка требований**: Админ может вручную редактировать любое требование в "Карте требований" (менять статус, дедлайн, ответственного).
- **Справочник НПА**: (`/admin/legal-acts`) CRUD-интерфейс для управления справочником нормативно-правовых актов.
- **"Движок правил" (Рефакторинг)**: (`/admin/rules`) **Сердце системы**. Вся логика генерации требований (23+ правила) вынесена из C#-кода в этот справочник. Администратор может изменять логику работы приложения (добавлять/менять требования и риски) через UI, без перекомпиляции Backend.

## 🏃 Быстрый запуск

Для запуска проекта не требуется внешняя база данных (используется `InMemoryDatabase`).

### 1. Запуск Backend (.NET API)

1. Перейдите в папку `EcologyLK.Api` (где находится `EcologyLK.Api.csproj`).
2. Выполните `dotnet restore` (для установки пакетов).
3. Выполните `dotnet run --launch-profile https`.

Сервер API будет запущен по адресу **`https://localhost:7166`**. При первом запуске `DataSeeder` автоматически заполнит `InMemoryDatabase` данными:

- Создаст роли "Admin" и "Client".
- Создаст пользователя "Admin" (см. ниже).
- Создаст "Тестового Клиента (ЮрЛицо)" с `Id = 1`.
- Заполнит "Справочник Правил" (23+ правила).

### 2. Запуск Frontend (Angular)

1. Перейдите в папку `EcologyLK.Client` (где находится `package.json`).
2. Выполните `npm install` (для установки пакетов, включая `@fullcalendar`).
3. Выполните `ng serve`.

Клиентское приложение будет доступно по адресу **`http://localhost:4200`**.

### 3. Тестовые пользователи

- **Администратор** (полный доступ ко всему, видит "Админ-панель"):
  - **Логин**: `admin@ecology.lk`
  - **Пароль**: `AdminP@ssw0rd1!`
- **Клиент** (ограниченный доступ):
  1. Войдите как **Администратор**.
  2. Перейдите в "Админ: Пользователи" (`/admin/users`).
  3. Создайте нового пользователя (напр., `client@test.ru`, пароль `Password123!`).
  4. **Важно:** В поле "Привязка к Клиенту" выберите "ООО Тестовый Клиент (ЮрЛицо) (ID: 1)".
  5. Назначьте роль "Client" и сохраните.
  6. Выйдите и войдите как `client@test.ru`. Этот пользователь будет видеть данные только для `ClientId = 1` (RLS в действии).
